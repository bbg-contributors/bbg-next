{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // no ambient @types: this is bundled into the browser runtime and every plugin
    "lib": ["es2023", "dom"],
    "types": []
  },
  "include": ["src"]
}
