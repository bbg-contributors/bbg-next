{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // no ambient types: core is bundled into the browser unchanged and must not reach a Node global
    "types": []
  },
  "include": ["src"]
}
